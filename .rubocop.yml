inherit_from:
    - .rubocop_todo.yml

# Custom configuration, edit with care!

# Fixing this issues cause some test to fail
Lint/UnifiedInteger:
  Enabled: false

Metrics/ClassLength:
  Max: 500
  Exclude:
  - test/**/*
